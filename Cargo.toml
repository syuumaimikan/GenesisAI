[workspace]
members = [
    "core",
    "runtime",
    "memory",
    "planning",
    "tools",
    "plugin-sdk",
    "agent-desktop",
    "npc-runtime"
]
resolver = "2"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"