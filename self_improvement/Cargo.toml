[package]
name = "genesis_self_improvement"
version = "0.1.0"
edition = "2024"

[dependencies]
genesis-core = { path = "../core" }
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
thiserror = "1.0"
tracing = "0.1"
uuid = { version = "1.6", features = ["v4", "serde"] }
petgraph = "0.6" # タスク実行DAG（有向非巡回グラフ）構築用
async-trait = "0.1"